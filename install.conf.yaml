# Dotbot configuration

- defaults:
    link:
      relink: true

- clean:
    ~/:
      force: true
    ~/.config:
      recursive: true

- link:
    ~/.config/bat: bat
    ~/.config/starship: starship
